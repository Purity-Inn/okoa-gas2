{
  "builds": [
    {
      "src": "package.json",
      "use": "@vitejs/plugin-react"
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*\\.(js|css|ico|png|jpg|svg|json|woff2?))", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
