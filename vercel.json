{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build",
        "zeroConfig": true,
        "installCommand": "npm install --prefix frontend"
      }
    }
  ]
}
